{% block extends %}
{{ "{% extends app.request.isxmlhttprequest ? '::base.html.twig' : 'RBShopBundle::index.html.twig' %}" }}
{% endblock extends %}

{% block body %}
    {{ '{% block body -%}' }}

    {{ '{{ form_start(edit_form) }}' }}

    {{ '{% for item in edit_form %}'}}
        {{ '{{ form_row(item) }}'}}
    {{ '{% endfor %}'}}

    {{ '{{ form_end(edit_form) }}' }}

{{ "{% endblock %}" }}
{% endblock body %}
