{% block extends %}
{{ "{% extends app.request.isxmlhttprequest ? '::base.html.twig' : 'RBAdminBundle::index.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block body -%}" }}

    {{ '{{ form_start(form) }}' }}
    {{ '{% set submit = form_widget(form.submit,{attr:{class:"btn btn-primary"}}) %}' }}
    {{ '{% for item in form %}'}}
    {{ '{{ form_row(item,{attr:{class:"form-control"}}) }}'}}
    {{ '{% endfor %}'}}
    <hr>
    {{ '{{submit | raw}}' }}

    {{ '{{ form_end(form) }}' }}

{{ "{% endblock %}" }}
{% endblock body %}
